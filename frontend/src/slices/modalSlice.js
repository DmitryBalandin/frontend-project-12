import { createSlice } from "@reduxjs/toolkit";


const initialState = {
    type: null,
    isShow:false
}

const modalSlice = createSlice ({
    name:'modal',
    initialState,
    reducers:{
        
    }
})